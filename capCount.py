# import sys

# def counter():
#     word = sys.argv[1]
#     listed_word = list(word)
#     indexs = [l for l in range(len(listed_word)) if listed_word[l].isupper()]
#     is_up = [l for l in listed_word if l.isupper()]

#     print(len(is_up))
#     print(sum(indexs[:]))

# counter()

for i in range(-2, 5):
    continue
    if i % 2 > 0:
        print(i)
    if  i % 2 < 0:
        print(i)